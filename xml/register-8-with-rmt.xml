<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE section
[
  <!ENTITY % entities SYSTEM "generic-entities.ent">
    %entities;
]>

<section xml:id="register-8-with-rmt" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.1"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink">
 <title>Registering &rhel; 8 with &rmt;</title>

 <info>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker></dm:bugtracker>
   <dm:translation>no</dm:translation>
  </dm:docmanager>
 </info>

 <para>
  Use this procedure to register &rhel; 8 or CentOS Linux 8 with the &rmt; server.
 </para>
 <para>
  CentOS Stream 8 is not supported.
 </para>
 <itemizedlist>
  <title>Prerequisites</title>
  <listitem>
   <para>
    <xref linkend="configure-rmt-server"/>
   </para>
  </listitem>
  <listitem>
   <para>
    <xref linkend="mirror-repositories-with-rmt"/>
   </para>
  </listitem>
  <listitem>
   <para>
    The system you want to register can access the &rmt; server.
   </para>
  </listitem>
  <listitem>
   <para>
    You have a &productname; subscription activated in the &scc;.
   </para>
  </listitem>
  <listitem>
   <para>
    You have removed the system from any &rh; subscription services it was
    registered to.
   </para>
  </listitem>
 </itemizedlist>
 <procedure xml:id="pro-register-8-with-rmt">
  <step>
   <para>
    Disable the &rhla; or CentOS repositories in <filename>/etc/yum.repos.d</filename>
    by setting <literal>enabled=0</literal> in each <filename>.repo</filename> file.
   </para>
  </step>
  <step>
   <para>
    Download the <filename>rmt-client-setup-res</filename> script:
   </para>
<screen>&prompt.root;<command>curl http://<replaceable>RMT_SERVER</replaceable>/tools/rmt-client-setup-res --output rmt-client-setup-res</command></screen>
  </step>
  <step>
   <para>
    Run the <filename>rmt-client-setup-res</filename> script with
    the URL of the RMT server as a parameter:
   </para>
<screen>&prompt.root;<command>sh rmt-client-setup-res https://<replaceable>RMT_SERVER</replaceable></command></screen>
   <para>
    The script performs the following tasks:
   </para>
   <itemizedlist>
    <listitem>
     <para>
      Installs <command>&suseconnect;</command> and its dependencies.
     </para>
     <remark>Fails due to conflicts between sles_es-release and centos-gpg-keys, centos-linux-repos, and centos-linux-release. It almost works when I manually add --allowerasing to the rmt-client-setup-res script, but then fails when /usr/share/redhat-release from sles_es-release conflicts with the file from centos-linux-release. It fully works when I also run `rm -r /usr/share/redhat-release` before running the script. I can add these workarounds as steps in this procedure, but it would be better if they (or some equivalent) were just in the script to begin with.</remark>
    </listitem>
    <listitem>
     <para>
      Downloads and runs the <filename>rmt-client-setup</filename> script, which
      does the following:
     </para>
     <itemizedlist>
      <listitem>
       <para>
        Downloads all keys from
        <literal>http://<replaceable>RMT_SERVER</replaceable>/repo/keys</literal>.
       </para>
      </listitem>
      <listitem>
       <para>
        Imports the keys with <command>gpg --import</command> and
        <command>rpm --import</command>.
       </para>
      </listitem>
      <listitem>
       <para>
        Downloads the CA certificate from
        <literal>https://<replaceable>RMT_SERVER</replaceable>/rmt.crt</literal>.
       </para>
      </listitem>
      <listitem>
       <para>
        Imports the CA certificate to the trust store.
       </para>
      </listitem>
      <listitem>
       <para>
        Uses <command>&suseconnect;</command> to register the system with the
        RMT server. Registering the system using <command>&suseconnect;</command>
        adds and enables all default repositories.
       </para>
      </listitem>
     </itemizedlist>
    </listitem>
   </itemizedlist>
  </step>
  <step>
   <para>
    Verify that the script worked:
   </para>
   <itemizedlist>
    <listitem>
     <para>
      List the installed products:
     </para>
<screen>&prompt.root;<command>&suseconnect; --status-text</command></screen>
     <para>
      You should see the following output:
     </para>
<screen>
Installed Products:
------------------------------------------

  SLES Expanded Support platform release file
  (RES/8/x86_64)

  Registered

------------------------------------------
</screen>
    </listitem>
    <listitem>
     <para>
      List the available repositories:
     </para>
<screen>&prompt.root;<command>dnf repolist</command></screen>
     <para>
      You should see <literal>RES-8-Updates</literal>, <literal>RES-AS-8-Updates</literal>,
      and <literal>RES-CB-8-Updates</literal>.
     </para>
    </listitem>
    <listitem>
     <para>
      Run the update command to make sure there are no errors:
     </para>
<screen>&prompt.root;<command>dnf update</command></screen>
    </listitem>
   </itemizedlist>
  </step>
  <step>
   <para>
    If your subscription includes the &ha; extension, activate it with the
    following command:
   </para>
<screen>&prompt.root;<command>&suseconnect; –p RES-HA/8/x86_64</command></screen>
   <tip role="compact">
    <para>
     To check whether the &ha; extension is available, run
     <command>&suseconnect; --list-extensions</command> and look for
     <literal>&sle; &ha; Extension with Expanded Support</literal>.
    </para>
   </tip>
  </step>
 </procedure>
 <para>
  You can now update your system from repositories provided by &productname;.
 </para>
</section>
