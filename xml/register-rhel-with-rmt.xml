<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE section
[
  <!ENTITY % entities SYSTEM "generic-entities.ent">
    %entities;
]>

<section xml:id="register-rhel-with-rmt" xml:lang="en"
 xmlns="http://docbook.org/ns/docbook" version="5.1"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink">
 <title>Registering &rhla; with &rmt;</title>

 <info>
  <dm:docmanager xmlns:dm="urn:x-suse:ns:docmanager">
   <dm:bugtracker></dm:bugtracker>
   <dm:translation>no</dm:translation>
  </dm:docmanager>
 </info>

 <para>
  Use this procedure to register &rhel; &productnumber; with the &rmt; server.
 </para>
 <important role="compact">
  <para>
   CentOS Stream is not supported.
  </para>
 </important>
 <itemizedlist>
  <title>Requirements</title>
  <listitem>
   <para>
    <xref linkend="configure-rmt-server"/>
   </para>
  </listitem>
  <listitem>
   <para>
    <xref linkend="mirror-repositories-with-rmt"/>
   </para>
  </listitem>
  <listitem>
   <para>
    The system you want to register can access the &rmt; server.
   </para>
  </listitem>
  <listitem>
   <para>
    The system you want to register is running the latest minor release of &rhla; &productnumber;.
   </para>
  </listitem>
  <listitem>
   <para>
    You have a &productname; subscription activated in the &scc;.
   </para>
  </listitem>
  <listitem>
   <para>
    You have removed the system from any &rh; subscription services it was
    registered to.
   </para>
  </listitem>
 </itemizedlist>
 <procedure xml:id="pro-register-rhel-with-rmt">
  <title>Registering &rhla; with &rmt;</title>
  <step>
   <!--para> (trichardson 2022-11-11: Temporarily replacing the regular steps until the new script is available in RMT)
    On the &rhla; system, download the <filename>rmt-client-setup-res</filename> script:
   </para>
<screen>&prompt.root;<command>curl http://<replaceable>&rmt;_SERVER</replaceable>/tools/rmt-client-setup-res -/-output rmt-client-setup-res</command></screen-->
   <para>
    On the &rhla; system, save the following script as <literal>rmt-client-setup-res</literal>,
    replacing <replaceable>RMT_SERVER</replaceable> with the name of the &rmt; server:
   </para>
<screen language="bash">#! /bin/sh

PARAMS=$@

/usr/bin/rpm --import http://<replaceable>RMT_SERVER</replaceable>/repo/SUSE/Updates/SLL/9/x86_64/update/repodata/repomd.xml.key
sed -i 's/^enabled=1/enabled=0/' /etc/yum.repos.d/*
rm -rf /usr/share/redhat-release
rm -rf /etc/dnf/protected.d/redhat-release.conf
/usr/bin/dnf config-manager --add-repo http://<replaceable>RMT_SERVER</replaceable>/repo/SUSE/Updates/SLL/9/x86_64/update
/usr/bin/dnf config-manager --add-repo http://<replaceable>RMT_SERVER</replaceable>/repo/SUSE/Updates/SLL-AS/9/x86_64/update
/usr/bin/dnf install --assumeyes --allowerasing sll-release
/usr/bin/dnf install --assumeyes SUSEConnect librepo
/usr/bin/dnf config-manager --set-disabled "<replaceable>RMT_SERVER</replaceable>_repo_SUSE_Updates_SLL_9_x86_64_update"
/usr/bin/dnf config-manager --set-disabled "<replaceable>RMT_SERVER</replaceable>_repo_SUSE_Updates_SLL-AS_9_x86_64_update"
/usr/bin/curl -s -S http://<replaceable>RMT_SERVER</replaceable>/tools/rmt-client-setup --output rmt-client-setup
sh rmt-client-setup $PARAMS</screen>
  </step>
  <step>
   <para>
    Run the <filename>rmt-client-setup-res</filename> script with
    the URL of the &rmt; server as a parameter:
   </para>
<screen>&prompt.root;<command>sh rmt-client-setup-res https://<replaceable>RMT_SERVER</replaceable></command></screen>
   <para>
    The script installs <command>&suseconnect;</command> and its dependencies,
    then downloads and runs the <filename>rmt-client-setup</filename> script,
    which performs the following tasks:
   </para>
   <itemizedlist>
    <listitem>
     <para>
      Downloads all keys from
      <literal>http://<replaceable>RMT_SERVER</replaceable>/repo/keys</literal>.
     </para>
    </listitem>
    <listitem>
     <para>
      Imports the keys with <command>gpg --import</command> and
      <command>rpm --import</command>.
     </para>
    </listitem>
    <listitem>
     <para>
      Downloads the CA certificate from
      <literal>https://<replaceable>RMT_SERVER</replaceable>/rmt.crt</literal>.
     </para>
    </listitem>
    <listitem>
     <para>
      Imports the CA certificate to the trust store.
     </para>
    </listitem>
    <listitem>
     <para>
      Uses <command>&suseconnect;</command> to register the system with the
      &rmt; server. Registering the system using <command>&suseconnect;</command>
      adds and enables all default repositories.
     </para>
    </listitem>
   </itemizedlist>
   <para>
    You should see <literal>Successfully registered system</literal>.
   </para>
  </step>
  <step>
   <para>
    Verify the installed product:
   </para>
<screen>&prompt.root;<command>&suseconnect; --status-text</command></screen>
   <para>
    You should see the following output:
   </para>
<screen>
Installed Products:
------------------------------------------

  &productname; release file
  (SLL/&productnumber;/x86_64)

  Registered

------------------------------------------
</screen>
  </step>
  <step>
   <para>
    Verify the available repositories:
   </para>
<screen>&prompt.root;<command>dnf repolist</command></screen>
   <para>
    You should see <literal>SLL-&productnumber;-Updates</literal>,
    <literal>SLL-AS-&productnumber;-Updates</literal>,
    and <literal>SLL-CB-&productnumber;-Updates</literal>.
   </para>
  </step>
  <step>
   <para>
    If your subscription includes the &ha; extension, activate it with the
    following command:
   </para>
<screen>&prompt.root;<command>&suseconnect; -p SLL-HA/&productnumber;/x86_64</command></screen>
   <tip role="compact">
    <para>
     To check whether the extension is available, run the
     <command>&suseconnect; --list-extensions</command> command.
    </para>
   </tip>
  </step>
  <step>
   <para>
    Run the update command to make sure there are no errors:
   </para>
<screen>&prompt.root;<command>dnf update</command></screen>
  </step>
 </procedure>
 <para>
  You can now update your system from repositories provided by &productname;.
 </para>
</section>
